@model HeaderLinksModel
@using Nop.Core
@using Nop.Core.Domain.Customers
@using Nop.Services.Configuration

@inject Nop.Core.IWebHelper webHelper
@inject IStoreContext storeContext
@inject ISettingService settingService



@{
    int storeId = (await storeContext.GetCurrentStoreAsync()).Id;
    bool compareProductsEnabled = await settingService.GetSettingByKeyAsync("catalogsettings.compareproductsenabled", true, storeId, true);
    bool shouldShowLoginFormInHeader = await settingService.GetSettingByKeyAsync("EmporiumThemeSettings.ShowLoginFormInHeader", false, storeId, true);

    var controllerName = Url.ActionContext.RouteData.Values["controller"].ToString();
    var actionName = Url.ActionContext.RouteData.Values["action"].ToString();

    var isLoginPage = controllerName.Equals("customer", StringComparison.InvariantCultureIgnoreCase) &&
                            actionName.Equals("login", StringComparison.InvariantCultureIgnoreCase);
}

<style>
    /* ===============================
   MY ACCOUNT DROPDOWN (RTL + LTR)
================================ */

.account-dropdown {
    position: relative;
}

    .currency-selector.desktop:before{
        display:none !important;
    }

    .language-selector.desktop .select-wrap:before{
        display: none !important;
    }

/* زر My Account */
.account-toggle {
    display: flex;
    align-items: center;
    gap: 6px;
    cursor: pointer;
    color: inherit;
    text-decoration: none;
}

/* صندوق القائمة */
.profile-menu-box {
    position: absolute;
    top: 100%;
    inset-inline-end: 0; /* RTL/LTR */
    background: #fff;
    min-width: 240px;
    border-radius: 12px;
    box-shadow: 0 12px 30px rgba(0,0,0,.18);
    display: none;
    flex-direction: column;
    z-index: 9999;
    padding: 8px 0;
}

/* فتح القائمة */
.profile-menu-box.open {
    display: flex;


  left:0px !important;
}

/* الروابط داخل الصندوق */
.profile-menu-box a {
    display: flex;
  
    align-items: center;
    gap: 10px;
    padding: 10px 16px;
    color: #222;
    text-decoration: none;
    text-align: start; /* RTL/LTR */
    white-space: nowrap;
}

.profile-menu-box a:hover {
    background: #f5f5f5;
}

/* فاصل */
.account-divider {
    height: 1px;
    background: #eee;
    margin: 6px 0;
}

/* Language & Currency wrapper */
.account-selector {
    position: relative;
    padding: 8px 16px;
}

/* select (لغة / عملة) */
.account-selector select {
    width: 100%;
    padding: 10px 12px;
    padding-inline-start: 12px;
    padding-inline-end: 36px;

    border-radius: 14px;
    border: 2px solid #e5e5e5;
    background-color: #fff;

    font-weight: 600;
    color: #6f6a77;

    text-align: start;

    /* إزالة سهم المتصفح */
    appearance: none;
    -webkit-appearance: none;
    -moz-appearance: none;

    background-image: none;
}


/* سهم select */
.account-selector::after {
    content: "▾";
    position: absolute;
    top: 50%;
    inset-inline-end: 24px;
    transform: translateY(-50%);
    pointer-events: none;
    font-size: 12px;
    opacity: .6;
}

/* Mini login form styles */
.header-form-holder {
    padding: 12px 16px;
    width: 280px;
    box-sizing: border-box;
}
.mini-login-form .mini-input {
    margin-bottom: 10px;
}
.mini-login-form input[type="email"],
.mini-login-form input[type="password"] {
    width: 100%;
    padding: 10px 12px;
    border-radius: 10px;
    border: 1px solid #e0e0e0;
    background-color: #fafafa;
    box-sizing: border-box;
    font-size: 14px;
}
.mini-login-form .mini-forgot {
    text-align: center;
    margin: 8px 0;
    font-size: 12px;
}
.mini-login-form .mini-forgot a {
    color: #888;
    text-decoration: underline;
}
.mini-login-form .mini-remember {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 12px;
    font-size: 13px;
}
.mini-login-form .mini-buttons {
    display: flex;
    flex-direction: column;
    gap: 8px;
}
.mini-login-form .button-1.login-button {
    background: #f4a740;
    color: #fff;
    border: none;
    padding: 10px;
    border-radius: 8px;
    cursor: pointer;
    font-weight: 700;
}
.mini-login-form .button-2.register-button {
    background: #f3f3f3;
    color: #666;
    text-align: center;
    padding: 10px;
    border-radius: 8px;
    text-decoration: none;
    display: inline-block;
    border: 1px solid #e6e6e6;
}
.mini-login-form .button-2.register-button:hover {
    background: #eee;
}
</style>

<div class="header-links">
    <ul>
        @await Component.InvokeAsync("Widget", new { widgetZone = PublicWidgetZones.HeaderLinksBefore, additionalData = Model })
        @if (Model.IsAuthenticated)
        {
            <li class="account-dropdown">
                <a href="javascript:void(0)" class="ico-account account-toggle">
                    @T("Account.MyAccount")
                </a>

                <div class="profile-menu-box">
                    <a href="@Url.RouteUrl("CustomerInfo")" class="ico-account">
                        @T("Account.MyAccount")
                    </a>

                    @if (Model.AllowPrivateMessages)
                    {
                        <a href="@Url.RouteUrl("PrivateMessages", new { tab = "" })" class="ico-inbox">
                            <span class="inbox-label">@T("PrivateMessages.Inbox")</span>
                            <span class="inbox-unread">@Model.UnreadPrivateMessages</span>
                        </a>
                    }

                    <div class="account-divider"></div>
                    <a href="@Url.RouteUrl("Logout")" class="ico-logout">
                        @T("Account.Logout")
                    </a>

                    <!-- Currency -->
                    <div class="account-selector">
                        @await Component.InvokeAsync("CurrencySelector")
                    </div>

                    <!-- Language -->
                    <div class="account-selector">
                        @await Component.InvokeAsync("LanguageSelector")
                    </div>

                    <div class="account-divider"></div>

                </div>

            </li>

        }
        else
        {
            var returnUrl = Context.Request.Query.TryGetValue("returnUrl", out var url) && !StringValues.IsNullOrEmpty(url)
                ? url.ToString()
                : webHelper.GetRawUrl(Context.Request);
            <li class="account-dropdown">
                @* For guests: open a mini login/register menu instead of navigating away. *@
                <a href="javascript:void(0)"
                   class="ico-account account-toggle opener"
                   data-login-url="@Url.Action("Login", "Customer")"
                   data-register-url="@Url.RouteUrl("Register", new { returnUrl })"
                   data-return-url="@returnUrl">
                    @T("Account.MyAccount")
                </a>

        <div class="profile-menu-box
                     @if (shouldShowLoginFormInHeader && !isLoginPage)
                     {
                         <text>login-form-in-header</text>
                     }">

            <div class="close-side-menu">
                <span class="close-side-menu-text">
                    @T("Account.MyAccount")
                </span>
                <span class="close-side-menu-btn">
                    @T("SevenSpikes.Themes.Common.CloseMenu")
                </span>
            </div>

            <div class="header-form-holder">
                @* Mini inline login form for guests *@
                <form asp-action="Login" asp-controller="Customer" method="post" autocomplete="off" class="mini-login-form" id="mini-login-form">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="returnUrl" value="@returnUrl" />
                    <div class="mini-input">
                        <input name="Email" type="email" placeholder="@T("Account.Login.Fields.Email")" required />
                    </div>
                    <div class="mini-input">
                        <input name="Password" type="password" placeholder="@T("Account.Login.Fields.Password")" required />
                    </div>

                    <div class="mini-forgot">
                        <a asp-route="PasswordRecovery">@T("Account.Login.ForgotPassword")</a>
                    </div>

                    <div class="mini-remember">
                        <input id="mini-remember" name="RememberMe" type="checkbox" />
                        <label for="mini-remember">@T("Account.Login.Fields.RememberMe")</label>
                    </div>

                    <div class="mini-buttons">
                        <button type="submit" class="button-1 login-button">@T("Account.Login.LoginButton")</button>

                        @if (Model.RegistrationType != UserRegistrationType.Disabled)
                        {
                            <a href="@Url.RouteUrl("Register", new { returnUrl })" class="button-2 register-button">
                                @T("Account.Register")
                            </a>
                        }
                    </div>
                </form>

                <script>
                    // Validate mini form client-side: if empty fields -> redirect to full login page with a flag
                    (function () {
                        var miniForm = document.getElementById('mini-login-form');
                        if (!miniForm) return;
                        miniForm.addEventListener('submit', function (e) {
                            try {
                                var email = (miniForm.querySelector('input[name="Email"]') || {}).value || '';
                                var pass = (miniForm.querySelector('input[name="Password"]') || {}).value || '';
                                if (!email.trim() || !pass.trim()) {
                                    e.preventDefault();
                                    var loginUrl = '@Url.RouteUrl("Login")';
                                    var url = loginUrl + (loginUrl.indexOf('?') === -1 ? '?' : '&') + 'input=missing';
                                    var ret = encodeURIComponent('@returnUrl');
                                    if (ret) url += '&returnUrl=' + ret;
                                    window.location.href = url;
                                    return false;
                                }
                            } catch (ex) {
                                // ignore and allow submit if something unexpected happens
                            }
                        });
                    })();
                </script>
            </div>
        </div>
            </li>




        }
        @if (compareProductsEnabled)
        {
            <li><a href="@Url.RouteUrl("CompareProducts")" class="ico-compare">@T("Products.Compare.List")</a></li>
        }

        @if (Model.WishlistEnabled)
        {
            <li>
                <a href="@Url.RouteUrl("Wishlist")" class="ico-wishlist">
                    <span class="wishlist-label">@T("Wishlist")</span>
                    @* <span class="wishlist-qty">@T("Wishlist.HeaderQuantity", Model.WishlistItems)</span> *@
                </a>
            </li>
        }
        @await Component.InvokeAsync("Widget", new { widgetZone = PublicWidgetZones.HeaderLinksAfter, additionalData = Model })
    </ul>
    @if (Model.ShoppingCartEnabled)
    {
        <div class="mobile-flyout-wrapper">
            <div class="close-side-menu">
                <span class="close-side-menu-text">@T("SevenSpikes.Themes.Emporium.Common.ShoppingCart")</span>
                <span class="close-side-menu-btn">@T("SevenSpikes.Themes.Emporium.Common.Close")</span>
            </div>
            @await Component.InvokeAsync("FlyoutShoppingCart")
        </div>
    }
    @if (Model.AllowPrivateMessages && !string.IsNullOrEmpty(Model.AlertMessage))
    {
        //new private message notification
        <script asp-location="Footer">
            $(document).ready(function () {
                displayPopupNotification('@Html.Raw(JavaScriptEncoder.Default.Encode(Model.AlertMessage))', 'success', false);
            });
        </script>
    }
    @if (Model.ShoppingCartEnabled)
    {
        <script asp-location="Footer">
            $(document).ready(function () {
                $('.header').on('mouseenter', '#topcartlink', function () {
                    $('#flyout-cart').addClass('active');
                });
                $('.header').on('mouseleave', '#topcartlink', function () {
                    $('#flyout-cart').removeClass('active');
                });
                $('.header').on('mouseenter', '#flyout-cart', function () {
                    $('#flyout-cart').addClass('active');
                });
                $('.header').on('mouseleave', '#flyout-cart', function () {
                    $('#flyout-cart').removeClass('active');
                });
            });
        </script>
    }
</div>
<script>
(function(){
    var toggles = document.querySelectorAll('.account-toggle');
    var openClass = 'open';
    var mobileBreakpoint = 768; // treat <=768 as mobile
    var isRtl = (document.documentElement.dir || document.body.dir || '').toLowerCase() === 'rtl';

    function closeAll(except){
        document.querySelectorAll('.profile-menu-box.'+openClass).forEach(function(m){
            if(m !== except) m.classList.remove(openClass);
        });
    }

    function positionMenu(toggle, menu){
        // clear previously set inline styles
        menu.style.left = '';
        menu.style.right = '';
        menu.style.width = '';
        menu.style.position = '';
        menu.style.top = '';

        var vw = window.innerWidth;
        if(vw <= mobileBreakpoint){
            // Mobile: make it full width and fixed under the header toggle
            var rect = toggle.getBoundingClientRect();
            menu.style.position = 'fixed';
            menu.style.top = (rect.bottom) + 'px';
            menu.style.left = '0';
            menu.style.right = '0';
            menu.style.minWidth = 'unset';
            menu.style.borderRadius = '0 0 12px 12px';
            menu.style.zIndex = 99999;
        } else {
            // Desktop / tablet: absolute aligned to the toggle's end (respecting RTL)
            menu.style.position = 'absolute';
            // ensure menu has a sensible min width
            var minW = Math.max(240, menu.offsetWidth || 240);
            menu.style.minWidth = minW + 'px';

            var rect = toggle.getBoundingClientRect();
            var menuWidth = menu.offsetWidth || minW;
            var spaceRight = window.innerWidth - rect.right;
            var spaceLeft = rect.left;

            // Prefer aligning to inset-inline-end (CSS already sets inset-inline-end:0),
            // but if it would overflow, adjust to keep it inside viewport
            if(isRtl){
                // align to end (right)
                if(spaceRight < menuWidth && spaceLeft > spaceRight){
                    // shift left so it fits
                    var shift = Math.min(spaceLeft - 8, menuWidth - spaceRight);
                    menu.style.right = (0 + shift) + 'px';
                } else {
                    menu.style.right = '0';
                }
            } else {
                // LTR: align to end (right side of the parent)
                if(spaceRight < menuWidth && spaceLeft > spaceRight){
                    var shiftL = Math.min(spaceLeft - 8, menuWidth - spaceRight);
                    // Position using left so it doesn't overflow
                    menu.style.left = Math.max(0, rect.left - shiftL) + 'px';
                } else {
                    // keep default end alignment
                    menu.style.right = '0';
                }
            }
        }
    }

    toggles.forEach(function(toggle){
        var menu = toggle.nextElementSibling;
        if(!menu || !menu.classList.contains('profile-menu-box')) return;

        // prevent clicks inside the menu from closing it
        menu.addEventListener('pointerdown', function(e){ e.stopPropagation(); });
        menu.addEventListener('click', function(e){ e.stopPropagation(); });

        // use pointerdown for faster response on touch devices
        toggle.addEventListener('pointerdown', function(e){
            e.preventDefault();
            var willOpen = !menu.classList.contains(openClass);
            closeAll(willOpen ? menu : null);
            if(willOpen){
                menu.classList.add(openClass);
                positionMenu(toggle, menu);
                // focus first focusable element for accessibility
                var focusable = menu.querySelector('a, button, input, [tabindex]:not([tabindex="-1"])');
                if(focusable) focusable.focus();
            } else {
                menu.classList.remove(openClass);
            }
        });

        // keyboard support on toggle
        toggle.addEventListener('keydown', function(e){
            if(e.key === 'Enter' || e.key === ' '){ e.preventDefault(); toggle.click(); }
        });

        // reposition on resize/scroll when open
        window.addEventListener('resize', function(){ if(menu.classList.contains(openClass)) positionMenu(toggle, menu); });
        window.addEventListener('scroll', function(){ if(menu.classList.contains(openClass)) positionMenu(toggle, menu); }, true);
    });

    // close when clicking or touching outside
    document.addEventListener('pointerdown', function(e){
        document.querySelectorAll('.profile-menu-box.'+openClass).forEach(function(menu){
            var parent = menu.parentElement;
            if(!parent.contains(e.target)) menu.classList.remove(openClass);
        });
    });

    // close on Escape
    document.addEventListener('keydown', function(e){
        if(e.key === 'Escape'){
            document.querySelectorAll('.profile-menu-box.'+openClass).forEach(function(menu){ menu.classList.remove(openClass); });
        }
    });
})();
</script>
